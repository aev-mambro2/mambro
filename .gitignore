# Generated by Cargo
# will have compiled files and executables
**/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
**/Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

#local db environment contains passwords which must not go into source control
.env

#And something creates swap files, so we ignore those, too.
**/*.swp

#And we don't want the contents of our data going out to public source control either.
data/*.*

