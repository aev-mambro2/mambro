[package]
name = "models"
version = "0.1.0"
authors = ["A.E.Veltstra"]
edition = "2018"
description = "Translates our data store structures into rust structures and back."

[dependencies]
component = "0.1.0"
diesel = { version = "1.4.0", features = ["sqlite"] }
dotenv = "0.9.0"
