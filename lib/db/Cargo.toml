[package]
name = "db"
version = "0.1.0"
authors = ["A.E.Veltstra"]
edition = "2018"
description = "Knows how to connect to our data store."

[dependencies]
component = "0.1.0"
diesel = { version = "1.4.0", features = ["sqlite"] }
dotenv = "0.9.0"
